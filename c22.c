/* a bit inconclusive */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[])
{
    if(argc!=1) {
        printf("No args for now\n"); 
        exit(EXIT_SUCCESS);
    }
    int i;
    char *c[2]={"uptime", sprintf(ZZm"uname"};
    for(i=0;i<2;++i) 
        printf("%s\n", c[i]);

    return 0;
}
